echo "# dk8s" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Autodotnet/dk8s.git


Comandos para ver status
  kind creata cluster --config cluster.yaml
  kind create cluster --config cluster.yaml
  kubectl get nodes
  kubectl get nodes
  kubectl get pods
  kubectl get namespace
  kubectl get pods -n kube-system
  kubectl get pods -n kube-system -o wide
  kubectl get pods -n kube-system -o
  kubectl get pods -n kube-system
  kubectl get deployment
  kubectl get replicase
  kubectl get replicast
  kubectl get replicaset
  kubectl get service -n kube-system
  kubectl exec -ti giropops -- bash

Comados para o bash completion

  apt install bash-completion
  kubectl completion --help
  kubectl completion bash > ~/.kube/completion.bash.inc
  echo "source '$HOME/.kube/completion.bash.inc" >> $HOME/.bash_profile
  vim /root/.bash_profile
  source /root/.bash_profile

  Comandos para criar run

  kubectl run --image nginx --port 80 -n giropops
  kubectl run --image nginx --port 80 --name giropops
  kubectl run --image nginx --port 80 -name giropops
  kubectl run --image nginx --port 80  giropops
  
Fazendo o dry run para criar o yaml

kubectl run --image nginx --port 80 giropops --dry-run=client -o yaml > pod.yaml

k apply -f pod.yaml

# Criando cluster usando o kind com pod nginx

kind create cluster --config kind-multinode-cluster.yaml
